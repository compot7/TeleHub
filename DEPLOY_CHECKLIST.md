# Чек-лист для деплоя на Render

## ✅ Подготовка проекта (выполнено)

- [x] Обновлен `settings.py` для работы с PostgreSQL и переменными окружения
- [x] Добавлен `dj-database-url` и `psycopg2-binary` в `requirements.txt`
- [x] Создан `Procfile` для запуска приложения
- [x] Обновлен `build.sh` для сборки проекта
- [x] Настроен WhiteNoise для статических файлов
- [x] Обновлен `urls.py` для раздачи медиа-файлов

## 📋 Шаги для деплоя

### 1. Подготовка репозитория
- [ ] Убедитесь, что проект загружен в GitHub/GitLab
- [ ] Все изменения закоммичены и запушены

### 2. Создание базы данных на Render
- [ ] Создать PostgreSQL базу данных
- [ ] Сохранить Internal Database URL

### 3. Создание Web Service
- [ ] Подключить репозиторий
- [ ] Указать:
  - Build Command: `./build.sh`
  - Start Command: `gunicorn telehub.wsgi:application --bind 0.0.0.0:$PORT`

### 4. Настройка переменных окружения
- [ ] `DATABASE_URL` - строка подключения к PostgreSQL
- [ ] `SECRET_KEY` - сгенерированный секретный ключ Django
- [ ] `DEBUG` - установить в `False`
- [ ] `ALLOWED_HOSTS` - домен вашего приложения (например: `telehub.onrender.com`)

### 5. После деплоя
- [ ] Проверить логи на наличие ошибок
- [ ] Создать суперпользователя через Shell:
  ```bash
  python manage.py createsuperuser
  ```
- [ ] Проверить работу сайта
- [ ] Проверить загрузку изображений

### 6. Опционально (рекомендуется для production)
- [ ] Настроить облачное хранилище для медиа-файлов (AWS S3, Cloudinary)
- [ ] Настроить кастомный домен
- [ ] Настроить мониторинг и алерты

## 🔗 Полезные ссылки

- Подробное руководство: [RENDER_DEPLOYMENT.md](RENDER_DEPLOYMENT.md)
- Render Dashboard: https://dashboard.render.com
- Документация Render: https://render.com/docs

## ⚠️ Важные замечания

1. **Медиа-файлы**: На бесплатном плане Render файлы удаляются при каждом деплое. Для production рекомендуется использовать облачное хранилище.

2. **SECRET_KEY**: Никогда не коммитьте реальный SECRET_KEY в репозиторий. Используйте переменные окружения.

3. **База данных**: Используйте Internal Database URL, а не External для лучшей производительности.

4. **Локальная разработка**: Для локальной разработки установите `DEBUG=True` через переменные окружения или напрямую в коде (но не коммитьте это изменение).


