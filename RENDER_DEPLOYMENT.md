# Руководство по развертыванию TeleHub на Render

Это руководство поможет вам развернуть проект TeleHub на платформе Render.com.

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Подготовка проекта](#подготовка-проекта)
3. [Создание аккаунта на Render](#создание-аккаунта-на-render)
4. [Создание PostgreSQL базы данных](#создание-postgresql-базы-данных)
5. [Создание веб-сервиса](#создание-веб-сервиса)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Деплой и проверка](#деплой-и-проверка)
8. [Настройка медиа-файлов](#настройка-медиа-файлов)
9. [Решения проблем](#решения-проблем)

---

## Предварительные требования

- Аккаунт на GitHub (или другом Git-хостинге)
- Проект должен быть загружен в Git-репозиторий
- Аккаунт на Render.com (можно создать бесплатно)

---

## Подготовка проекта

Проект уже подготовлен для деплоя. Убедитесь, что следующие файлы присутствуют:

- ✅ `Procfile` - файл для запуска приложения
- ✅ `build.sh` - скрипт сборки проекта
- ✅ `requirements.txt` - зависимости Python
- ✅ `runtime.txt` - версия Python

Все необходимые изменения в `settings.py` уже внесены:
- Поддержка PostgreSQL через переменные окружения
- WhiteNoise для статических файлов
- Правильная настройка DEBUG и SECRET_KEY

---

## Создание аккаунта на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email адрес

---

## Создание PostgreSQL базы данных

1. В Dashboard Render нажмите **"New +"** → **"PostgreSQL"**

2. Заполните форму:
   - **Name**: `telehub-db` (или любое другое имя)
   - **Database**: `telehub` (можно оставить по умолчанию)
   - **User**: `telehub_user` (можно оставить по умолчанию)
   - **Region**: Выберите ближайший регион (например, Frankfurt для Европы)
   - **PostgreSQL Version**: `16` (или последняя стабильная)
   - **Plan**: Выберите `Free` план (для начала)

3. Нажмите **"Create Database"**

4. Дождитесь создания базы данных (обычно 1-2 минуты)

5. **Сохраните строку подключения:**
   - После создания БД найдите секцию **"Connections"**
   - Скопируйте **"Internal Database URL"** - она понадобится позже
   - Формат: `postgresql://user:password@host:port/database`

---

## Создание веб-сервиса

1. В Dashboard Render нажмите **"New +"** → **"Web Service"**

2. Подключите репозиторий:
   - Если используете GitHub, нажмите **"Connect GitHub"**
   - Разрешите доступ к репозиторию
   - Выберите репозиторий с проектом TeleHub

3. Заполните форму создания сервиса:

   **Основные настройки:**
   - **Name**: `telehub` (или другое имя)
   - **Region**: Выберите тот же регион, что и для БД
   - **Branch**: `main` (или ваша основная ветка)
   - **Runtime**: `Python 3`
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn telehub.wsgi:application --bind 0.0.0.0:$PORT`

   **План:**
   - Выберите **"Free"** план для начала (можно обновить позже)

4. Нажмите **"Create Web Service"**

---

## Настройка переменных окружения

После создания веб-сервиса нужно настроить переменные окружения:

1. Перейдите на страницу созданного веб-сервиса
2. Откройте вкладку **"Environment"**
3. Добавьте следующие переменные:

### Обязательные переменные:

| Ключ | Значение | Описание |
|------|----------|----------|
| `DATABASE_URL` | `postgresql://...` | Строка подключения из PostgreSQL (Internal Database URL) |
| `SECRET_KEY` | Случайная строка | Сгенерируйте секретный ключ Django (см. ниже) |
| `DEBUG` | `False` | Отключить режим отладки в production |
| `ALLOWED_HOSTS` | `your-app-name.onrender.com` | Замените на имя вашего приложения |

### Генерация SECRET_KEY:

Выполните в терминале:
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

Или используйте онлайн-генератор:
- https://djecrety.ir/

### Пример настройки переменных:

```
DATABASE_URL=postgresql://telehub_user:password@dpg-xxxxx-a/telehub
SECRET_KEY=django-insecure-ваш-сгенерированный-ключ-здесь
DEBUG=False
ALLOWED_HOSTS=telehub.onrender.com
```

**Важно:** После добавления переменных окружения Render автоматически перезапустит сервис.

---

## Деплой и проверка

1. После создания сервиса Render автоматически начнет деплой
2. Дождитесь завершения сборки (обычно 3-5 минут)
3. Проверьте логи на вкладке **"Logs"**:
   - Убедитесь, что миграции выполнены успешно
   - Проверьте, что `collectstatic` завершился без ошибок
   - Убедитесь, что сервер запустился

4. Откройте ваш сайт по адресу:
   ```
   https://your-app-name.onrender.com
   ```

5. Создайте суперпользователя:
   - В Render Dashboard откройте вкладку **"Shell"** вашего сервиса
   - Выполните команду:
     ```bash
     python manage.py createsuperuser
     ```
   - Следуйте инструкциям для создания администратора

6. Проверьте работу сайта:
   - Откройте главную страницу
   - Зарегистрируйтесь
   - Создайте визитку
   - Проверьте загрузку изображений

---

## Настройка медиа-файлов

⚠️ **ВАЖНО:** На Render файловая система **эпиhemeral** (непостоянная). Все файлы, загруженные пользователями, будут удалены при каждом деплое или перезапуске сервиса.

### Варианты решения:

#### 1. Облачное хранилище (рекомендуется для production)

**AWS S3:**
```bash
pip install django-storages boto3
```

В `settings.py`:
```python
# settings.py
INSTALLED_APPS = [
    # ... existing apps ...
    'storages',
]

AWS_ACCESS_KEY_ID = os.environ.get('AWS_ACCESS_KEY_ID')
AWS_SECRET_ACCESS_KEY = os.environ.get('AWS_SECRET_ACCESS_KEY')
AWS_STORAGE_BUCKET_NAME = os.environ.get('AWS_STORAGE_BUCKET_NAME')
AWS_S3_REGION_NAME = os.environ.get('AWS_S3_REGION_NAME', 'us-east-1')

DEFAULT_FILE_STORAGE = 'storages.backends.s3boto3.S3Boto3Storage'
```

**Cloudinary (проще в настройке):**
```bash
pip install cloudinary django-cloudinary-storage
```

В `settings.py`:
```python
INSTALLED_APPS = [
    # ... existing apps ...
    'cloudinary',
    'cloudinary_storage',
]

CLOUDINARY_STORAGE = {
    'CLOUD_NAME': os.environ.get('CLOUDINARY_CLOUD_NAME'),
    'API_KEY': os.environ.get('CLOUDINARY_API_KEY'),
    'API_SECRET': os.environ.get('CLOUDINARY_API_SECRET'),
}

DEFAULT_FILE_STORAGE = 'cloudinary_storage.storage.MediaCloudinaryStorage'
```

#### 2. Render Persistent Disk (платная опция)

Если у вас платный план, можно использовать Persistent Disk для хранения медиа-файлов.

#### 3. Временное решение для тестирования

Для тестирования можно оставить текущую настройку, но помните, что файлы будут удаляться при каждом деплое.

---

## Решения проблем

### Проблема: Статические файлы не загружаются

**Решение:**
- Убедитесь, что WhiteNoise добавлен в `MIDDLEWARE`
- Проверьте, что `collectstatic` выполняется в `build.sh`
- Проверьте `STATIC_ROOT` в settings.py

### Проблема: Ошибка подключения к базе данных

**Решение:**
- Убедитесь, что используете **Internal Database URL**, а не External
- Проверьте, что переменная `DATABASE_URL` правильно установлена
- Убедитесь, что веб-сервис и БД в одном регионе

### Проблема: 500 Internal Server Error

**Решение:**
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что `DEBUG=False` и есть правильный `SECRET_KEY`
- Проверьте, что миграции выполнены: `python manage.py migrate`

### Проблема: Медиа-файлы не отображаются

**Решение:**
- Проверьте, что `MEDIA_URL` и `MEDIA_ROOT` правильно настроены
- Убедитесь, что медиа-файлы раздаются через `urls.py`
- Помните: на бесплатном плане файлы удаляются при деплое

### Проблема: Приложение не запускается

**Решение:**
- Проверьте `Procfile` - команда должна быть правильной
- Убедитесь, что используется переменная `$PORT`
- Проверьте логи на наличие ошибок импорта

### Проблема: Ошибка "DisallowedHost"

**Решение:**
- Добавьте ваш домен в `ALLOWED_HOSTS`
- Формат: `your-app-name.onrender.com`
- Или используйте: `.onrender.com` для всех поддоменов

---

## Полезные команды через Shell

В Render Dashboard → Shell можно выполнить Django команды:

```bash
# Создать суперпользователя
python manage.py createsuperuser

# Выполнить миграции (обычно выполняется автоматически)
python manage.py migrate

# Собрать статические файлы (обычно выполняется автоматически)
python manage.py collectstatic --noinput

# Создать резервную копию данных
python manage.py dumpdata > backup.json

# Загрузить данные из резервной копии
python manage.py loaddata backup.json
```

---

## Обновление приложения

После каждого коммита в репозиторий Render автоматически:
1. Определит изменения
2. Запустит `build.sh`
3. Перезапустит приложение

Вы можете настроить автодеплой только для определенной ветки в настройках сервиса.

---

## Мониторинг и логи

- **Logs**: Просмотр логов в реальном времени в Dashboard
- **Metrics**: Мониторинг использования ресурсов (CPU, память)
- **Events**: История деплоев и событий

---

## Обновление до платного плана

Если ваш проект растет, рассмотрите возможность обновления:
- **Starter Plan ($7/мес)**: Больше ресурсов, лучше производительность
- **Professional Plan ($25/мес)**: Dedicated ресурсы, лучше для production

---

## Дополнительные ресурсы

- Документация Render: https://render.com/docs
- Документация Django Deployment: https://docs.djangoproject.com/en/stable/howto/deployment/
- Django Best Practices: https://docs.djangoproject.com/en/stable/misc/design-philosophies/

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте документацию Render
3. Создайте issue в репозитории проекта

**Удачи с деплоем! 🚀**


